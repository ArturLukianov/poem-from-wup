#!/usr/bin/env python3
import sys
import parser

if len(sys.argv) < 2:
    print(sys.argv[0], ' <whatsapp chat>')
    exit(-1)

file = open(sys.argv[1])
messages = parser.parse(file.read())
file.close()

print(messages[:2])
